define(['underscore', 'backbone', 'src/models/BaseModel'], function(_, Backbone, BaseModel) {
	var WordsModel =  Backbone.Model.extend({
		initialize: function() {},		
   });
   return WordsModel;
});